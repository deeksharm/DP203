Post class assignment:
5/5/2026
1. Diffrence between Datalake vs Database vs datawarehouse vs datamart?
2. CRUD operation in sql
3. install ssms in the system
4. Write sql query to identify on which date mostly ordered has been placed.
5. Write sql query to identify which customer has done max sale.
6. Write sql query to identify which category product has been sold mostly.
7. diffrence between view, table & stored procedure.

6/5/2026
1. diffrence between view & materialised view
2. Normalization & SCD
3. CTE

7/5/2026
Exercise for hdfs & map reduce:
Create /tmp/<yourname> in HDFS
Upload employees.csv to it
Print it with -cat — verify all 10 rows
Confirm replication = 3 in the -ls output
Check size with -du -s -h
Change replication to 2 with -setrep and verify

8/5/2026
Write a city counter MapReduce job using Python streaming
Input: employees.csv in HDFS
Mapper: skip the header, emit city\t1 for each row
Reducer: count employees per city
Expected output: London\t1, Leeds\t1, Manchester\t1, etc.
Tak 2 : print non null records from null_mapper & null_reducer file
Task3: print output of employee with band column.

11/5/2026
column oriented storage or file format & parquet file format
Create external table itc_runbook.<yourname>_emp table
Load employees data into it
Query: find all employees aged over 30
Query: highest-paid employee in each city
Create a partitioned version and inspect the HDFS structure under /warehouse

12/5/2026
Run sqoop list-tables — verify you see cc_fraud_trans
Import the table to HDFS /tmp/<yourname>/sqoop_out
Use hdfs dfs -cat to preview the first 5 rows
Count the records: hdfs dfs -cat /tmp/<yourname>/sqoop_out/part-m-00000 | wc -l
Expected: ~7,400 rows (37,000 / 4 mappers)
Import Directly into Hive instant of HDFS 
Save a Sqoop Job (reuse across runs

13/5/2026
In Beeline (Hive), create itc_runbook.<yourname>_orders: order_id, product, amount, region
Insert 5 rows using INSERT INTO ... VALUES (...)
Switch to Impala shell, run INVALIDATE METADATA
Query the table — note the speed difference
Run COMPUTE STATS then EXPLAIN SELECT ... to see the query plan
read customersourcedemo.csv using spark
load into dataframe & take count of record & add one id column which has incrmenetal value.

14/5/2026
read customersourcedemo.csv using spark
load into dataframe & conver Header as lowercase
FirstName+MiddleName+LastName = fullname
Phone col to be cast int & remove additional character then keep last 10 digit
Timestamp col to be casted as timestamp
update gender col M=Male, F=Female
remove Suffix col
add one id column which has incrmenetal value.

15/5/2026
Open a PySpark shell on the cluster
Read employees.csv from HDFS into a DataFrame
Add a senior column: True if age >= 35, else False
Calculate average salary for senior vs junior employees
Write the result as Hive table itc_runbook.<yourname>_analysis
Verify with beeline: SELECT * FROM itc_runbook.<yourname>_analysis
--------------
Create a Kafka topic: <yourname>_test with 3 partitions
Write a Python producer that sends 10 employee records as JSON
Write a Python consumer that reads them and prints only salary > 50000
Check consumer lag with kafka-consumer-groups --describe
Delete the topic when done

--------------------------19/5/2026---------------
1. create blob storage
2. create ADLS
3. in blob & adls create container = raw, refined, proccessed 
4. create folders raw/2026/may then upload file customersourcedemo.csv (any file)
5. connect with your both storage using storage explorer locally & upload file from local then validate in portal
6. explore doffrent authentication menthod & diffrence between both storages

useful link:
https://data-flair.training/blogs/hadoop-tutorial/
https://www.databricks.com/blog/what-is-hadoop
https://data-flair.training/blogs/hadoop-mapreduce-tutorial/
